// Mortgage amortisation engine — see spec in wakeley-calc-apps-handoff.md §6
export function amortize(loanAmount, annualRate, termMonths, offsetSchedule = [], extraRepaymentSchedule = []) {
  // TODO: implement full engine (variable/fixed/split, offset, lump sums)
  return [];
}
