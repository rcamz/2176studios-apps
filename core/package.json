{
  "name": "@2176studios/core",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Tests run from the web workspace, which owns the vitest install\" && exit 0"
  },
  "description": "Platform-agnostic calculation core shared by the web build and each standalone app",
  "type": "module",
  "exports": {
    ".": "./src/index.js",
    "./rates": "./src/rates/index.js",
    "./*": "./src/*.js"
  }
}
