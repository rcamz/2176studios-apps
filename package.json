{
  "name": "2176studios-apps",
  "private": true,
  "workspaces": [
    "core",
    "web",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "test": "npm run test --workspace=web",
    "version:snapshot": "node scripts/version-snapshot.mjs",
    "seo:generate": "node scripts/generate-seo.mjs",
    "app:health": "node scripts/build-app.mjs health"
  }
}
