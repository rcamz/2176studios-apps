// Lives in @2176studios/core: the store listings are app metadata, needed by
// each standalone build and by any listing-upload script, not just the web app.
export * from '../../../core/src/appMeta.js';
